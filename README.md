# CronoSlot 3.0.3

Fix puntual de compilación tras el log de 3.0.2:
- Restaurados los helpers `labelsPilots`, `labelsCars`, `labelsTracks`.
- Restaurado `label(String)`.
- El error anterior consistía únicamente en referencias a esos métodos ausentes en `MainActivity.java`.

No se modifica la implementación de cámara.
